{
  "frontend/src/App.tsx": "Main application component handling auth flow, profile setup, and data migration — fixes the `UserRole` type error by casting the string result from `useGetCallerUserRole()` to `UserRole` using `as UserRole` instead of a direct assignment.",
  "frontend/src/pages/LoginPage.tsx": "Login page component with Internet Identity authentication and branded DroneWash interface.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard with lazy-loaded tab panels — removed the invalid `isAdmin` prop from `<ExpensesManagement />` (which takes no props) while keeping all other tab wiring and the `<MaintenanceManagement />` call without props intact.",
  "frontend/src/components/FinancialMetrics.tsx": "Financial overview component that now uses `useGetMaintenanceFundBalance()` (the ledger-derived canonical source) for the Maintenance Fund card balance instead of the stale `funds` array balance field — eliminating the €72,200 desync — while keeping all other metric cards, expandable breakdowns, and fund management dialogs intact.",
  "frontend/src/components/DataEntryForms.tsx": "Side-by-side forms for adding jobs and expenses with inline tables displaying recent entries including unique system-generated ID numbers shown as badges, full CRUD operations, and additional information field available for all expense categories.",
  "frontend/src/components/QuoteCalculator.tsx": "Interactive quote calculator with manual \"Estimated Hours\" input field for COGS calculations, updated cost formulas (Generator Fuel €7/hour, Additional Pilot €350/job), service-specific area tier determination, complete COGS breakdown display with city-based van fuel, and profit margin analysis with backend integration.",
  "frontend/src/components/QuotesList.tsx": "Quotes list component with search/filter, expandable rows, and PDF export using correct function names `generateQuotePDF` (customer) and `generateInternalQuotePDF(quote, costBreakdown)` (internal) from pdfGenerator.",
  "frontend/src/components/InvoiceCreation.tsx": "Invoice creation form with customer details, quote selection correctly showing service types from services array, summary preview with sub-sector badge, and automatic PDF generation with proper service data population.",
  "frontend/src/components/AnalyticsCharts.tsx": "Analytics charts component with memoized chart components (MemoizedBarChart, MemoizedLineChart) for performance optimization, useMemo for data transformations, side-by-side revenue vs expenses bar chart and 3-year profit projection line chart with error handling, retry buttons, loading states, empty state messages, and theme-aware chart styling for robust data display.",
  "frontend/src/components/ReportingSection.tsx": "Reporting section with cash flow export, unit economics table, and comprehensive error handling with retry functionality to ensure data availability and user feedback during backend issues.",
  "frontend/src/hooks/useQueries.ts": "Comprehensive React Query hooks for all backend operations — adds `useGetMaintenanceFundBalance` as the single canonical hook calling `actor.getMaintenanceFundBalance()` with query key `['maintenanceFundBalance']`, and ensures all mutations that affect the maintenance fund (addJob, updateJob, deleteJob, addMaintenanceFundInflow, addMaintenanceExpense, resetMaintenanceFund) invalidate both `['maintenanceFundBalance']` and `['maintenanceFundStatus']` for full cross-component synchronization.",
  "frontend/src/lib/pdfGenerator.ts": "PDF generation utilities using browser-native print functionality with separate functions for customer-facing quotes, internal quotes with COGS breakdown, and invoices with proper service calculations showing area (m²), price per m², and correctly calculated amounts (area × unit price) with two decimal place rounding.",
  "frontend/src/lib/reportGenerator.ts": "Report generation utilities for exporting cash flow and unit economics data as CSV files and printable HTML reports without external PDF libraries.",
  "frontend/src/index.css": "Global styles with OKLCH color system customized for DroneWash brand using cyan/blue accent colors (#0891b2) for both light and dark modes.",
  "frontend/src/components/EditJobDialog.tsx": "Modal dialog component for editing basic job entries (client name, revenue, date, sector) with backend integration for update operations, now properly passing cleanProfit and costs to maintain data integrity.",
  "frontend/src/components/EditExpenseDialog.tsx": "Modal dialog component for editing expense entries with backend integration, including additional information field available for all categories.",
  "frontend/src/components/DeleteConfirmDialog.tsx": "Reusable alert dialog component for confirming delete operations with loading state support.",
  "frontend/src/components/InvoicesList.tsx": "Displays all invoices in a table with advanced filtering and search functionality including text search, payment status filter, collapsible filter panel, results counter, payment status toggle, and role-based delete functionality.",
  "frontend/src/components/ProfileSetupDialog.tsx": "Modal dialog for first-time user profile setup with name, email, and role selection using UserRole enum values (admin/user).",
  "frontend/src/components/JobCalendar.tsx": "Interactive calendar component displaying all planned jobs from backend with month navigation, showing client names on calendar entries, and opening job details dialog when entries are clicked.",
  "frontend/src/components/JobDetailsDialog.tsx": "Modal dialog component for viewing and editing job details with standardized job type options (Façade/Wall Cleaning, Full Building Cleaning, Roof Cleaning, Solar Panel Cleaning, Window Cleaning), customer information, financial details, and role-based edit permissions.",
  "frontend/src/components/PricingManagement.tsx": "Admin-only pricing management component displaying editable rate tables for all five unified services with direct text/numeric input fields supporting free-form typing, service-specific area tier ranges, real-time backend updates, and validation with improved input handling and mobile keyboard support.",
  "frontend/src/components/MonthlyReports.tsx": "Monthly reporting component with period selector, KPI metrics grid, and goals tracker — fixed to pass `selectedYear` as `number` directly to `useMonthlyReport` and `useMonthlyGoalsByMonthYear` (removing the incorrect `BigInt` conversion at the call site, since the hooks accept `number` and handle the conversion internally).",
  "frontend/src/components/ArchivedInvestments.tsx": "Information component explaining the Investment Fund system with a navigation button that accepts an optional callback prop for reliable navigation to the Investment Fund tab.",
  "frontend/src/components/FundsManagement.tsx": "Multi-fund management component with overview, transactions, and transfers tabs — fixed `AddFundTransactionDialog` usage to pass both `fundId` and `fundName` props by tracking the full target object in state.",
  "frontend/src/components/MaintenanceManagement.tsx": "Maintenance fund management component that computes `currentBalance = totalCollected - totalSpent` client-side (fixing the stale €72,200 balance bug), shows three overview cards, a ledger entries table, an \"Add Inflow\" button (admin-only) alongside the existing \"Add Expense\" button, and the danger-zone Reset Fund AlertDialog.",
  "frontend/src/components/ExpensesManagement.tsx": "New advanced expense management component with search, filtering, bulk selection, category filtering, and comprehensive expense tracking with fund assignment support.",
  "frontend/src/components/EditFundDialog.tsx": "Modal dialog for editing fund details — fixes the missing `isActive` property error by passing `isActive: fund.isActive` through to `updateFund.mutateAsync`, preserving the fund's existing active state.",
  "frontend/src/components/AddFundTransactionDialog.tsx": "Modal dialog component for adding transactions to funds with support for inflow/outflow types, amount, category, and description fields with backend integration.",
  "frontend/src/components/AddMaintenanceExpenseDialog.tsx": "Dialog for logging maintenance expenses — fixed by removing the `receiptUrl` field from the `addExpense` mutation call params (it's not part of the hook's parameter type), while keeping the UI field for display purposes.",
  "frontend/src/components/CreateFundDialog.tsx": "Dialog for creating new funds — fixed to use `string` type for `fundType` state instead of the backend `FundType` enum to avoid type mismatch with the local `useCreateFund` hook.",
  "frontend/src/components/AddGoalDialog.tsx": "Dialog for adding new monthly goals — fixes the type error by passing `year` as a plain `number` (not `BigInt(year)`) to `addGoal.mutateAsync`, since the `useAddGoal` hook accepts `number` and handles the BigInt conversion internally.",
  "frontend/src/components/InvestmentFundManagement.tsx": "Investment fund management component displaying initial capital, current balance, payment/allocation totals, and transaction history with `AddInvestmentTransactionDialog` using `onOpenChange` prop.",
  "frontend/src/components/AddInvestmentTransactionDialog.tsx": "Dialog component for adding Investment Fund transactions with payment/allocation type selection, amount, allocation type, and detailed description fields with backend integration.",
  "frontend/src/components/DataMigrationHandler.tsx": "Automatic data migration component that transfers legacy localStorage data to the blockchain backend — fixes the type error by passing `goal.year ?? new Date().getFullYear()` as a plain `number` instead of `BigInt(...)` to `addGoal.mutateAsync`.",
  "frontend/src/components/LoadingPanel.tsx": "Reusable loading panel with spinner — `title` prop is now optional so it can be used as a Suspense fallback without requiring a title.",
  "frontend/src/components/ErrorPanel.tsx": "Reusable error panel component displaying error messages with retry functionality for failed data operations.",
  "frontend/src/components/MobileNav.tsx": "Mobile-optimized navigation drawer component using Sheet UI with touch-friendly navigation for all dashboard sections, automatic closing after selection, and role-based menu items.",
  "frontend/src/components/KPIWidgets.tsx": "Dynamic KPI widget dashboard displaying real-time metrics including net profit trend with sparklines, job completion performance, VAT due calculations, and fund balance deltas with visual trend indicators.",
  "frontend/src/components/UserPreferencesDialog.tsx": "User preferences dialog for dark mode, layout, and metric filters — updated prop interface from `onClose` to `onOpenChange` to match the pattern used throughout the app.",
  "frontend/src/components/COGSSettings.tsx": "New admin-only COGS configuration component for managing operational cost parameters including van fuel rates by city, generator fuel rate per hour, additional pilot fee per job, and chemicals cost per hour with persistent storage and real-time updates.",
  "frontend/src/components/BackupVersionsDialog.tsx": "Dialog component displaying all saved backup versions with BigInt-safe serialization for downloads, timestamp display, data size calculation, and version management with delete functionality.",
  "frontend/src/components/ContractsManagement.tsx": "Contract management component with CRUD operations for client contracts including contract details, services covered, area tiers, agreed pricing, and status tracking with search and filtering capabilities, now using correct DeleteConfirmDialog props.",
  "frontend/src/hooks/usePdfAttachments.ts": "Frontend-only IndexedDB utility module that provides async functions (savePdf, getPdf, deletePdf, hasAttachment) for storing and retrieving PDF files keyed by transaction ID in a local 'QuotationsFundPdfs' database — no backend calls are made.",
  "frontend/src/hooks/useActor.ts": "Initializes the backend actor by resolving the correct canister ID from environment variables or canister_ids.json (preferring the `ic` network entry for production and `local` for development), then creates the actor with the appropriate IC host URL and the current user's identity.",
  "frontend/vite.config.ts": "Vite build configuration that reads canister IDs from both `canister_ids.json` (production/ic) and `.dfx/local/canister_ids.json` (local dev), injects them as both `CANISTER_ID_*` and `VITE_CANISTER_ID_*` environment variables, and sets up the React plugin, path aliases, and local API proxy.",
  "frontend/src/components/AddMaintenanceInflowDialog.tsx": "New dialog component for manually adding inflow entries to the Maintenance Fund with amount and description fields, form validation, loading state, and success/error toasts via sonner."
}